# M00 — Repo + planning docs

**Status:** done
**Depends on:** –
**Goal:** `hermes-android` is a git repo containing this plan as trackable docs.

## Tasks

- [x] `git init` in `D:\Stuff\Code\git\hermes-android` (branch `main`)
- [x] `.gitignore` (node_modules, android/, ios/, .expo/, keystores, .env*)
- [x] `project-planning/implementation-plan/README.md` tracker with status column
- [x] One file per milestone `M00` … `M12` (goal, tasks, deliverables, exit criteria)
- [x] `AGENTS.md`: dial-only invariant, read-only upstream, vendored-code rule, reauth ladder, one-time credentials, source tag rule
- [x] Initial commit

## Deliverables

- `.gitignore`, `AGENTS.md`
- `project-planning/implementation-plan/README.md` + `M00` … `M12`

## Exit criteria

- `git log` shows the initial commit.
- The tracker renders with all milestones `todo` except M00.
